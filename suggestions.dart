// lib/screens/suggestions.dart
import 'package:flutter/material.dart';

class SuggestionsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Suggestions for Energy Usage'),
      ),
    );
  }
}

